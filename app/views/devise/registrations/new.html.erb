<span class="text-center">
	<h2>Lab Member sign-up</h2>
  <h4><%= link_to "Group leaders sign up here", new_group_leader_registration_path %></h4>
</span>
<br>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name),
                              :html => { :class => 'form-vertical'} ) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, :required => true, :autofocus => true, label: "Your email address",
    							placeholder: "Institutional addresses only", input_html: { style: 'width: 350px' } %>
    
    <%= f.association :lab, value_method: :room, include_blank: false,
    									      label: "Group leader's email address",
                            input_html: { style: 'width: 350px' },
                            wrapper_html: { class: 'GLemail' },
                            placeholder: "Institutional addresses only" %>
    <br>
    <%= f.input :password, :required => true, input_html: {:style=> 'width: 350px'} %>
    <%= f.input :password_confirmation, :required => true, input_html: {:style=> 'width: 350px'} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
