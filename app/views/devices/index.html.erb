<%- model_class = Device -%>
<div class="page-header">
  <% if @lab %>
    <h4>The <%= link_to "#{@lab.name} Lab's", @lab %> devices</h4>  
  <% elsif params[:user_id].present? %>        
    <h4><%= link_to "#{current_user.fullname}'s", current_user %> devices</h4>
  <% end %>

  <% if params[:tag] %>
    <h3>devices tagged with <%= "#{params[:tag]}" %></h3>
  <% end %>
</div>

<% if params[:user_id].present? %>
  <%= form_tag user_devices_path(current_user.lab), method: :get do %>
    <%= text_field_tag :search, params[:query],
                                class: "navbar-search",
                                placeholder: "Search by name, tag, UID or serial"  %>
  <% end %>
<% elsif params[:lab_id].present? %>
  <%= form_tag lab_devices_path, method: :get do %>
    <%= text_field_tag :search, params[:query],
                                class: "navbar-search",
                                placeholder: "Search by name, tag, UID or serial"  %>
  <% end %>
<% end %>
<br><br><br>
  <%= link_to " Add new device", new_lab_device_path(current_user.lab), class: 'fa fa-plus-square btn btn-warning' %>
  <br><br>
  <%= render 'devices' %>

<%= will_paginate @devices, class: 'text-center lead' %>
