$('#paginator-top').html('<%= escape_javascript(paginate(@devices,
																												 remote: true,
																												 theme: "twitter-bootstrap", 
                                       									 pagination_class: "pagination-small pagination-centered").to_s) %>');
$('#paginator-bottom').html('<%= escape_javascript(paginate(@devices,
																												    remote: true,
																												 	  theme: "twitter-bootstrap", 
                                       									    pagination_class: "pagination-small pagination-centered").to_s) %>');

$('#devices').html('<%= escape_javascript(render("devices")) %>');
