$('#notifications').html('<%= escape_javascript(render("notifications")) %>');

$('#paginator-top').html('<%= escape_javascript(paginate(@notifications,
																												 remote: true, 
																												 theme: "twitter-bootstrap", 
                                       									 pagination_class: "pagination-small pagination-centered").to_s) %>');

$('#paginator-bottom').html('<%= escape_javascript(paginate(@notifications,
																														remote: true,
																														theme: "twitter-bootstrap", 
                                       											pagination_class: "pagination-small pagination-centered").to_s) %>');