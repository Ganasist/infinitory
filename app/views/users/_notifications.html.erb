<% if @user.comments.present? %>
<h3>Notifications</h3><br>
  <table>
  	<% @comments.each do |comment| %>     
			<tr class="user_notifications">
				<%= time_ago_in_words(comment.created_at) %> ago<br>
				<%= comment.comment %>
			</tr>
			<hr>
		<% end %>
	</table>
<% end %>