<%= simple_form_for(@institute, html: {class: 'form-vertical' }) do |f| %>
  <% if @institute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institute.errors.count, "error") %> prohibited this institute from being saved:</h2>

      <ul>
      <% @institute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :name, label: 'Institute name' %><br>
  <%= f.input :alternate_name, placeholder: 'Optional' %><br>
  <%= f.input :address %><br>
  <%= f.button :submit %>

<% end %>
