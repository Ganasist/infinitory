<% cache institute do %>
	<div class="institute">
		<% if institute.acronym.present? %>
			<p><%= link_to "#{institute.name}" " " "(#{institute.acronym})" , institute_path(institute) %><p> 
		<% else %>
			<p><%= link_to institute.name, institute_path(institute) %><p> 
		<% end %>
	</div>
<% end %>