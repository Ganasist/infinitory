<% if @device.present? %>
	Updated by
<% else %>
	Updated
	<% if activity.trackable %>
		<%= link_to "#{activity.trackable.fullname}", activity.trackable %>
	<% else %>
	  a device which has since been removed
	<% end %>
<% end %>