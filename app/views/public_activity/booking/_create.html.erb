Created a booking
<% if activity.trackable && activity.trackable.device %>
	for <%= link_to "#{activity.trackable.device.fullname}", activity.trackable.device %>
<% elsif activity.trackable && !activity.trackable.device %>
	for a device which has since been removed
<% else %>
	which has since been removed
<% end %>