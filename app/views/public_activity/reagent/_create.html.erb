<% if @reagent.present? %>
	Created by
<% else %>
	Created
	<% if activity.trackable %>
		<%= link_to "#{activity.trackable.fullname}", activity.trackable %>
	<% else %>
	  a reagent which has since been removed
	<% end %>
<% end %>