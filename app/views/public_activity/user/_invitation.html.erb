Invited
<% if activity.trackable %>
	<%= activity.trackable.role.humanize.downcase %> <%= link_to activity.trackable.fullname, activity.trackable %>
<% else %>
  a lab member (who has since been quit) 
<% end %>