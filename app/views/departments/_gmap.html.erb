<%= gmaps(map_options: { auto_zoom: true,
												 disableDefaultUI: true,
												 mapTypeControl: true,
												 maxZoom: 17,
												 minZoom: 2},
          markers: { "data" => @mapped }) %>