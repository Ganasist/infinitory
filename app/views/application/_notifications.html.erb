<% @comments.each do |comment| %>
	<p class="list-group-item-heading lead"><%= time_ago_in_words(comment.created_at) %> ago</p>
	<h4 class="text-group-item-text"><%= comment.comment %></h4>
	<hr>
<% end %>