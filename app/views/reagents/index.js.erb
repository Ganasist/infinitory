$('#reagents').html('<%= escape_javascript(render("reagents")) %>');

$('#paginator-top').html('<%= escape_javascript(paginate(@reagents,
																												 remote: true,
																												 theme: "twitter-bootstrap", 
                                       									 pagination_class: "pagination-small pagination-centered").to_s) %>');

$('#paginator-bottom').html('<%= escape_javascript(paginate(@reagents,
																												 		remote: true,
																												 		theme: "twitter-bootstrap", 
                                       									 		pagination_class: "pagination-small pagination-centered").to_s) %>');
